int add(int i, int k);
int sub(int i, int k);
int mul(int i, int k);
int div(int i, int k);
int mod(int i, int k);

/**
 * add - adds two integers
 * @i: first integer to add
 * @k: second integer to add
 * Return: the sum
 */

int add(int i, int k)
{
	return (i + k);
}

/**
 * sub - subtracts two integers
 * @i: first integer to substract
 * @k: second integer to substract
 * Return: the substract
 */

int sub(int i, int k)
{
	return (i - k);
}

/**
 * mul - multiplies two integers
 * @i: first integer to multiply
 * @k: second integer to multiply
 * Return: the multiplication
 */

int mul(int i, int k)
{
	return (i * k);
}

/**
 * div - divides two integers
 * @i: first integer to divide
 * @k: second integer to divide
 * Return: the division
 */

int div(int i, int k)
{
	return (i / k)
}

/**
 * mod - finds the modulus of two integers
 * @i: first integer to mod
 * @k: second integer to mod
 * Return: the modulus
 */

int div(int i, int k)
{
        return (i % k)
}
